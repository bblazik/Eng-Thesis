<h1> New benefit </h1>
<%= form_for :benefit, url: benefit_index_path do |f| %>
  <p>
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Back', benefit_index_path %>